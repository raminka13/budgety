<h1>Editing expense</h1>

<%= render "form", expense: @expense %>

<br>

<div>
  <%= link_to "Show this expense", category_expense_path(@category, @expense) %> |
  <%= link_to "Back to expenses", category_expenses_path %>
</div>
