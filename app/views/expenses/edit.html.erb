<h1>Editing expense</h1>

<% if can? :manage, @expense %>
  <%= render "form", category: @expense %>
<% else %>
    <h2>Not your CATEGORY. Go Back.</h2>
<% end %>
<br>

<div>
  <%= link_to "Back to Category", category_path(@category) %>
</div>
